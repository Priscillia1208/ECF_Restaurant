{% extends 'base.html.twig' %}

{% block title %}Formule{% endblock %}

{% block body %}
    <h1>Formule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formule.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ formule.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ formule.prix }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ formule.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_formule_index') }}">back to list</a>

    <a href="{{ path('app_admin_formule_edit', {'id': formule.id}) }}">edit</a>

    {{ include('admin_formule/_delete_form.html.twig') }}
{% endblock %}
